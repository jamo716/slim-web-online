import Typography from "@material-ui/core/Typography";
import Container from "@material-ui/core/Container"

function About() {

    return (
      <Container>
        <Typography variant="h3">SLiM Web In Development</Typography>
        <Typography variant="body1">This tool allows for remote rendering of select SLiM scripts over the web!</Typography>
      </Container>
    );
  }
  
  export default About;
  